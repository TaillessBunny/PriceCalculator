import { Estimate } from './estimate';
describe('Estimate', () => {
    it('should create an instance', () => {
        expect(new Estimate()).toBeTruthy();
    });
});
//# sourceMappingURL=estimate.spec.js.map